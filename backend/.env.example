# =================================
# ESXi-Mate - 环境变量示例
# =================================

# =================================
# 应用配置
# =================================
APP_HOST=0.0.0.0
APP_PORT=9601
DEBUG=True

# =================================
# 数据库配置（默认 SQLite 零配置启动）
# =================================
# 不设置时默认：sqlite:///./esxi_mate.db
DATABASE_URL=sqlite:///./esxi_mate.db

# 若使用 MySQL（可选），示例：
# DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/esxi_mate?charset=utf8mb4
# DB_POOL_SIZE=10
# DB_MAX_OVERFLOW=20

# =================================
# CORS 配置
# =================================
# 允许的跨域源，多个用逗号分隔
CORS_ORIGINS=http://localhost:9528,http://127.0.0.1:9528

# =================================
# ESXi 虚拟化接入（单机直连）
# =================================
# 默认凭证（未在 DB 中填写密码时回退使用）
ESXI_USER=root
ESXI_PASSWORD=your_esxi_password

# 可选：初始主机列表（逗号分隔，用于脚本导入）
# ESXI_HOSTS=172.16.63.11,172.16.63.12
